this code is run by the ConnectionRefusedError
# this code is run by the   "detail": "The specified city does not exist in the database.",
from pydantic import BaseModel, Field               
                "timestamp": "2024-01-15T14:30:00"
            }
        }                   

create a new file named weather.py in the api/models directory
"""     Pydantic models for weather data
""" 
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime   
class ErrorResponse(BaseModel):fff
rgf
